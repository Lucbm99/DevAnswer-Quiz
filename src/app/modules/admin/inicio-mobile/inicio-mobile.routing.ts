import { Route } from '@angular/router';
import { InicioMobileComponent } from './inicio-mobile.component';

export const rotaPaginaMobile: Route[] = [
    {
        path     : '',
        component: InicioMobileComponent
    }
];