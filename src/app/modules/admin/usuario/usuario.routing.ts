import { Route } from '@angular/router';
import { UsuarioComponent } from './usuario.component';

export const rotaUsuario: Route[] = [
    {
        path     : '',
        component: UsuarioComponent
    }
];