import { Route } from '@angular/router';
import { PerguntasComponent } from './perguntas.component';

export const rotasPerguntas: Route[] = [
    {
        path     : '',
        component: PerguntasComponent
    }
];